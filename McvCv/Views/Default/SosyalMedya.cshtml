@using McvCv.Models.Entity
@model List<TblSocialMedia>

<div class="social-icons">
    @foreach (var x in Model)
    {
    <a class="social-icon" href="@x.link"  target="_blank"><i class="@x.icon"></i></a>

    }
</div>
